{
  "name": "parkersphysics",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node dev-server.mjs",
    "dev:8080": "PORT=8080 node dev-server.mjs",
    "build": "bash build-wasm.sh",
    "data:snapshots": "node scripts/heliochronicles/build-snapshots.mjs",
    "data:verify": "node scripts/heliochronicles/verify.mjs",
    "test": "npx playwright test",
    "test:gravity": "node js/gravity-lab/test/run.mjs",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --headed --debug",
    "test:ui": "npx playwright test --ui",
    "test:cloud-live": "CLOUD_LIVE=1 npx playwright test tests/cloud-live.spec.js --project=chromium",
    "fixtures:clouds": "node scripts/fetch-cloud-fixtures.mjs --probe",
    "probe:dashboard": "node scripts/probe-dashboard.mjs",
    "probe:dashboard:local": "node scripts/probe-dashboard.mjs http://localhost:8000/dashboard.html"
  },
  "dependencies": {
    "@vercel/edge": "^1.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "playwright": "^1.56.0"
  },
  "engines": {
    "node": "22.x"
  }
}
